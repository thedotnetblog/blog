<?xml version="1.0" encoding="utf-8" standalone="yes"?><rss version="2.0" xmlns:atom="http://www.w3.org/2005/Atom" xmlns:content="http://purl.org/rss/1.0/modules/content/"><channel><title>Vscode | The .NET Blog</title><link>https://thedotnetblog.com/tags/vscode/</link><description>Articles, tutorials and insights from the .NET community.</description><generator>Hugo</generator><language>en</language><managingEditor>@thedotnetblog (The .NET Blog)</managingEditor><webMaster>@thedotnetblog</webMaster><lastBuildDate>Thu, 03 Sep 2026 00:00:00 +0000</lastBuildDate><atom:link href="https://thedotnetblog.com/tags/vscode/index.xml" rel="self" type="application/rss+xml"/><item><title>The Coding Harness: Why the Model Matters Less Than You Think</title><link>https://thedotnetblog.com/news/emiliano-montesdeoca/copilot-coding-harness-vscode/</link><pubDate>Thu, 03 Sep 2026 00:00:00 +0000</pubDate><author>Emiliano Montesdeoca</author><guid>https://thedotnetblog.com/news/emiliano-montesdeoca/copilot-coding-harness-vscode/</guid><description>The coding harness assembles context, exposes tools, runs the agent loop, and evaluates behavior. Understanding that layer is central to reliable .NET workflows in VS Code.</description><content:encoded>&lt;p&gt;Original source: &lt;a href="https://code.visualstudio.com/blogs/2026/05/15/agent-harnesses-github-copilot-vscode"&gt;The Coding Harness Behind GitHub Copilot in VS Code&lt;/a&gt;&lt;/p&gt;
&lt;p&gt;Every few months, the question resurfaces: which model is best, fastest, or most capable? Those are useful questions, but for an agent operating inside VS Code, the model is only one part of the experience.&lt;/p&gt;
&lt;p&gt;The coding harness is the layer that assembles context, exposes tools, runs the agent loop, interprets tool calls, and turns model output into edits and validation inside the editor. The model is the engine. The harness is the system that determines whether the engine can actually move the work forward.&lt;/p&gt;
&lt;p&gt;For .NET developers relying on Copilot for code generation, refactoring, and debugging, this explains why two models can behave differently in the same repository and why a model upgrade does not automatically produce a better development workflow.&lt;/p&gt;
&lt;h2 id="three-responsibilities-of-the-harness"&gt;Three Responsibilities of the Harness&lt;/h2&gt;
&lt;p&gt;The source identifies three main responsibilities.&lt;/p&gt;
&lt;p&gt;&lt;strong&gt;Context assembly&lt;/strong&gt; builds the prompt before it reaches the model. It can include system instructions, the user&amp;rsquo;s request, workspace structure, open editors, conversation history, tool results, custom instructions, and memory from earlier sessions. The harness decides what the model sees, so repository guidance is part of the agent&amp;rsquo;s input rather than decorative documentation.&lt;/p&gt;
&lt;p&gt;For a .NET project, this is why an instruction file should mention architecture, testing, package policy, and commands. If the agent cannot see the rule at the moment it makes a decision, the rule is not reliably part of the workflow.&lt;/p&gt;
&lt;p&gt;&lt;strong&gt;Tool exposure&lt;/strong&gt; declares what the model may call: reading files, editing code, searching the repository, running terminal commands, and using tools contributed by MCP servers or extensions. Each tool has a schema and description that influences whether and how the model invokes it. Custom agents can restrict the tool set to a narrower subset.&lt;/p&gt;
&lt;p&gt;A well-structured .NET solution gives these tools something useful to operate on. A reproducible &lt;code&gt;dotnet build&lt;/code&gt;, a targeted test command, clear project boundaries, and fast feedback make the agent loop more effective.&lt;/p&gt;
&lt;p&gt;&lt;strong&gt;Tool execution&lt;/strong&gt; validates arguments, runs the tool, handles errors, formats the result, and feeds it back to the next iteration. When an agent requests a file edit or a terminal command, the language model is not performing that action directly. The harness is the boundary that applies the change and reports what happened.&lt;/p&gt;
&lt;h2 id="the-agent-loop"&gt;The Agent Loop&lt;/h2&gt;
&lt;p&gt;The core pattern is a think, act, observe, and think-again cycle. The harness builds the prompt, sends it to the model, checks the response, executes any tool calls, captures the results, and loops again. If there are no more tool calls, the turn can finish.&lt;/p&gt;
&lt;p&gt;A user-visible turn may contain many rounds. The agent can search for a project file, read it, edit a test, run &lt;code&gt;dotnet test&lt;/code&gt;, inspect a failure, and revise the change before returning one answer. The workspace state is rebuilt into later prompts, so the model can work from the result of its earlier actions.&lt;/p&gt;
&lt;p&gt;The loop has controls: tool-call limits, cancellation checks, stop hooks, and conversation summarization. When the history grows too large, earlier rounds are compressed so the agent can continue within its context window. Those controls are not implementation trivia. They determine how much autonomy a task can safely have and how recoverable a failure is.&lt;/p&gt;
&lt;h2 id="models-need-harness-tuning"&gt;Models Need Harness Tuning&lt;/h2&gt;
&lt;p&gt;VS Code supports models from multiple providers and needs to accommodate differences in tool calling, structured outputs, reasoning controls, prompt caching, context limits, and error behavior. Some models are better at long planning; others are better at concise edits.&lt;/p&gt;
&lt;p&gt;The source gives concrete examples: Claude models use &lt;code&gt;replace_string_in_file&lt;/code&gt;, GPT models use &lt;code&gt;apply_patch&lt;/code&gt;, Gemini needs reminders to use tool-calling instead of narrating it, and different models receive different system prompts. A new model cannot simply be added to a picker and assumed to work well. Tool schemas, defaults, prompts, and full sessions need validation.&lt;/p&gt;
&lt;p&gt;This is useful context for .NET teams comparing models. Test the model in the workflow you actually care about. A model that performs well on a small coding benchmark may be poor at a multi-project migration, a browser interaction, or a long sequence of terminal checks.&lt;/p&gt;
&lt;h2 id="evaluation-keeps-the-harness-honest"&gt;Evaluation Keeps the Harness Honest&lt;/h2&gt;
&lt;p&gt;Public benchmarks such as SWE-bench and Terminal-Bench are useful reference points, but they do not cover every real editor workflow. Developers also need agents to scaffold projects, refactor across files, follow repository instructions, and use terminals, browsers, and extensions.&lt;/p&gt;
&lt;p&gt;VS Code built VSC-Bench as an offline evaluation suite for product-specific tasks including custom agent modes, extension workflows, MCP and tool use, terminal and browser interaction, multi-turn conversations, and multi-language coding. It measures dimensions such as solution correctness, agent effort, token efficiency, and latency.&lt;/p&gt;
&lt;p&gt;Each task runs in a reproducible, containerized workspace. The harness launches VS Code, sends prompts, allows tool calls, and evaluates what happened. That is closer to the experience developers actually use than judging only the final text or patch.&lt;/p&gt;
&lt;p&gt;The team also benchmarks harness changes before they merge. Pull requests that receive the evaluation label can be built, published as an evaluation agent, benchmarked, and reported back to the pull request. This treats changes to tools, prompts, or loop behavior as product changes that need evidence.&lt;/p&gt;
&lt;h2 id="what-this-means-for-net-developers"&gt;What This Means for .NET Developers&lt;/h2&gt;
&lt;ol&gt;
&lt;li&gt;Improve repository context before switching models. Instructions and acceptance criteria shape what the harness can assemble.&lt;/li&gt;
&lt;li&gt;Keep build and test feedback fast enough for an agent to use repeatedly.&lt;/li&gt;
&lt;li&gt;Compare models on representative .NET tasks and record correctness, review effort, latency, and token use.&lt;/li&gt;
&lt;li&gt;Inspect the Chat Debug View when a tool call or context decision looks wrong.&lt;/li&gt;
&lt;li&gt;Protect terminal and deployment boundaries with branch protection, approvals, and environment-specific identities.&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;The model still matters. But the practical result comes from the model, the context it receives, the tools it can reach, the loop that keeps it working, and the evaluations that prove the combination is useful. That combined system is the coding harness, and it is where reliable agent development is built.&lt;/p&gt;</content:encoded></item><item><title>VS Code 1.113: Agent Workflows, Chat Customization, and the Thinking Model</title><link>https://thedotnetblog.com/news/emiliano-montesdeoca/vscode-1-113-release/</link><pubDate>Sun, 30 Aug 2026 00:00:00 +0000</pubDate><author>Emiliano Montesdeoca</author><guid>https://thedotnetblog.com/news/emiliano-montesdeoca/vscode-1-113-release/</guid><description>VS Code 1.113 brings unified chat customizations, configurable thinking effort, nested subagents, and MCP support to CLI agent workflows.</description><content:encoded>&lt;p&gt;Original source: &lt;a href="https://code.visualstudio.com/updates/v1_113"&gt;Visual Studio Code 1.113&lt;/a&gt;&lt;/p&gt;
&lt;p&gt;VS Code 1.113 represents a meaningful shift in how agents operate inside the editor. This release moves beyond incremental improvements and addresses the friction .NET developers hit when orchestrating multi-step workflows, composing specialized agents, and reasoning through complex problems.&lt;/p&gt;
&lt;h2 id="chat-customizations-one-place-to-manage-agent-guidance"&gt;Chat Customizations: One Place to Manage Agent Guidance&lt;/h2&gt;
&lt;p&gt;If you&amp;rsquo;ve been configuring VS Code agents, you know the pain. Custom instructions here, prompt files there, agent definitions in another folder. VS Code 1.113 introduces the Chat Customizations editor, a unified interface for creating and managing custom instructions, prompt files, custom agents, and agent skills.&lt;/p&gt;
&lt;p&gt;For .NET developers, this matters immediately. You can use one interface to inspect the repository&amp;rsquo;s architectural patterns, dependency policies, and testing conventions instead of hunting through several customization types. The editor includes syntax highlighting and validation, which should mean fewer misconfigurations when an agent starts behaving in an unexpected way.&lt;/p&gt;
&lt;p&gt;The editor can also use AI to generate initial customization content based on the project. That is useful for getting a first draft of a repository guide, but it should remain a draft. If your .NET repository requires integration tests for database mutations or has strict async naming conventions, review the generated instructions against the actual code and build checks before committing them.&lt;/p&gt;
&lt;p&gt;The feature is in preview. Treat it as a consolidation opportunity, not a reason to abandon version control. Keep customization files in the repository, review the diff, and make sure a teammate can understand what guidance an agent will receive.&lt;/p&gt;
&lt;h2 id="thinking-effort-spend-reasoning-where-it-pays"&gt;Thinking Effort: Spend Reasoning Where It Pays&lt;/h2&gt;
&lt;p&gt;Models that support reasoning, such as Claude Sonnet 4.6 and GPT-5.4, now expose a Thinking Effort submenu directly in the model picker. The available levels can vary by model, and VS Code retains the selected effort level per model across conversations. The picker label also shows the active level, for example &lt;code&gt;GPT-5.3-Codex - Medium&lt;/code&gt;.&lt;/p&gt;
&lt;p&gt;This is practical for .NET work because not every task deserves the same latency or token budget. A cross-project refactor, an authorization review, or a difficult concurrency bug may justify a higher reasoning level. Repetitive boilerplate, straightforward test scaffolding, or a mechanical rename usually does not.&lt;/p&gt;
&lt;p&gt;My recommendation is to treat thinking effort as a workflow setting, not a badge. Run the same kind of task at two levels and compare correctness, elapsed time, token use, and review effort. The most expensive setting is not automatically the most useful one, and the cheapest setting is not a win if it creates another debugging pass.&lt;/p&gt;
&lt;p&gt;The release also deprecates older reasoning-effort settings in favor of the model picker. That makes the active choice more visible, which is helpful when comparing results across sessions.&lt;/p&gt;
&lt;h2 id="nested-subagents-more-composition-more-responsibility"&gt;Nested Subagents: More Composition, More Responsibility&lt;/h2&gt;
&lt;p&gt;Subagents can now invoke other subagents when &lt;code&gt;chat.subagents.allowInvocationsFromSubagents&lt;/code&gt; is enabled. Previously, subagents were restricted from making these invocations to prevent infinite recursion. The new setting opens more complex multi-step workflows.&lt;/p&gt;
&lt;p&gt;A .NET code-review agent could delegate a focused security question to a security subagent. A migration agent could hand a specific compatibility issue to a specialist while keeping responsibility for the overall plan. This is useful when the task has genuinely different areas of expertise.&lt;/p&gt;
&lt;p&gt;It also creates a new failure mode. Without a clear delegation boundary, nested agents can loop, duplicate work, or consume time without producing a better result. Define which agent may delegate, what information it passes, and what completion signal ends the chain. Start with one level of nesting and inspect the debug log before adding more.&lt;/p&gt;
&lt;p&gt;Composition works best when each subagent has a narrow contract. A security subagent should report findings and evidence, not silently rewrite the migration. A test subagent should explain what it validated, not declare an architectural decision complete.&lt;/p&gt;
&lt;h2 id="cli-agents-and-shared-mcp-tools"&gt;CLI Agents and Shared MCP Tools&lt;/h2&gt;
&lt;p&gt;MCP servers configured in VS Code can now be bridged to Copilot CLI and Claude agents, including user-defined servers and workspace servers in &lt;code&gt;mcp.json&lt;/code&gt;. That makes a repository&amp;rsquo;s custom tools more portable across local editor and command-line workflows.&lt;/p&gt;
&lt;p&gt;For a .NET team, this could mean sharing a domain-specific validator, an internal API lookup, or a build diagnostic tool across the places where agents run. The portability is valuable, but the permission boundary still matters. Review which MCP servers are safe for automation, what data they can reach, and whether they should be available in CI.&lt;/p&gt;
&lt;p&gt;The release also brings forking sessions to Copilot CLI and Claude agents, behind the relevant Copilot CLI setting, and extends the Agent Debug Log panel to those sessions in preview. That gives teams more ways to explore an alternative prompt and inspect the prompts, tool calls, and event history that produced a result.&lt;/p&gt;
&lt;h2 id="what-net-developers-should-do-now"&gt;What .NET Developers Should Do Now&lt;/h2&gt;
&lt;ol&gt;
&lt;li&gt;Consolidate chat customizations carefully, keep the files versioned, and review preview behavior.&lt;/li&gt;
&lt;li&gt;Test thinking effort on representative tasks and measure review quality, not just response speed.&lt;/li&gt;
&lt;li&gt;Design nested delegation with explicit ownership and a clear stopping condition.&lt;/li&gt;
&lt;li&gt;Audit MCP servers before sharing them with CLI or CI agents.&lt;/li&gt;
&lt;li&gt;Use debug logs and forked sessions to investigate behavior instead of relying on a successful-looking final answer.&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;The important change in 1.113 is composability. Agents, tools, customizations, and reasoning controls are becoming parts of one workflow. That makes the workflow more capable, but it also makes repository guidance and observability more important than ever.&lt;/p&gt;</content:encoded></item><item><title>How VS Code Builds with AI: Lessons from Weekly Releases</title><link>https://thedotnetblog.com/news/emiliano-montesdeoca/vscode-builds-with-ai/</link><pubDate>Thu, 27 Aug 2026 00:00:00 +0000</pubDate><author>Emiliano Montesdeoca</author><guid>https://thedotnetblog.com/news/emiliano-montesdeoca/vscode-builds-with-ai/</guid><description>The VS Code team's AI-assisted development workflow offers practical lessons in parallel work, issue triage, automated validation, and human judgment.</description><content:encoded>&lt;h1 id="how-vs-code-builds-with-ai-lessons-from-weekly-releases"&gt;How VS Code Builds with AI: Lessons from Weekly Releases&lt;/h1&gt;
&lt;p&gt;Original source: &lt;a href="https://code.visualstudio.com/blogs/2026/03/13/how-VS-Code-Builds-with-AI"&gt;How VS Code Builds with AI&lt;/a&gt;&lt;/p&gt;
&lt;p&gt;The VS Code team moved from monthly to weekly releases, and the interesting lesson is not simply that agents write code quickly. The team uses them across triage, review, release notes, validation, and task management, while keeping human judgment at the points where product and architecture matter.&lt;/p&gt;
&lt;p&gt;That is a useful pattern for .NET teams because it puts the focus on the system around the model. Faster generation without better validation only produces faster regressions.&lt;/p&gt;
&lt;h2 id="parallelize-the-work-around-interruptions"&gt;Parallelize the Work Around Interruptions&lt;/h2&gt;
&lt;p&gt;A developer who is about to enter a meeting can start several bounded agent sessions: investigate a failing test, inspect a deprecated API, draft a pull request description, or prototype a small change. Those tasks can run in separate sessions, worktrees, or cloud environments while the developer is unavailable.&lt;/p&gt;
&lt;p&gt;The key is to prepare the tasks before starting them. Each session needs a target, an expected output, and a boundary on what it may change. When the developer returns, the results are ready for review rather than still waiting for a first command.&lt;/p&gt;
&lt;p&gt;For a .NET team, this could mean delegating an inventory of obsolete package references, a scan of missing API tests, and a review of a build failure in parallel. The agents do not need to agree; the human can compare evidence once the work is collected.&lt;/p&gt;
&lt;h2 id="automate-the-overhead-that-follows-speed"&gt;Automate the Overhead That Follows Speed&lt;/h2&gt;
&lt;p&gt;More commits and issues create more administrative work. The VS Code workflow uses agents to summarize commits, triage issues, detect likely duplicates, suggest ownership, and draft release information.&lt;/p&gt;
&lt;p&gt;The lesson is to automate work where the input is plentiful and the decision can remain reviewable. A triage suggestion is useful when it includes its confidence and evidence. A generated release note is useful when a maintainer can edit it before publishing.&lt;/p&gt;
&lt;p&gt;.NET teams can apply the same pattern to areas such as ASP.NET, Entity Framework, MAUI, or Azure Functions. Let an agent collect signals and prepare a first pass. Keep final ownership, labels, and release wording under human control.&lt;/p&gt;
&lt;h2 id="replace-some-documents-with-working-prototypes"&gt;Replace Some Documents with Working Prototypes&lt;/h2&gt;
&lt;p&gt;The source describes a workflow where an issue or product idea can become a working pull request prototype. That shortens the loop between feedback and something concrete that engineers can run.&lt;/p&gt;
&lt;p&gt;A prototype is not automatically an accepted design. It is a more informative conversation starter than a document that has never been tested. Engineers still need to challenge the architecture, inspect the diff, and reject a change that does not fit the product.&lt;/p&gt;
&lt;p&gt;This is also a test of whether a repository is agent-ready. Can the agent find the relevant components? Can it run the right checks? Can another developer understand the generated pull request? If not, the missing structure or documentation is useful feedback for the team.&lt;/p&gt;
&lt;h2 id="validation-is-the-harness"&gt;Validation Is the Harness&lt;/h2&gt;
&lt;p&gt;The team&amp;rsquo;s workflow emphasizes automated validation, browser-based checks, golden scenarios, and code review. That is the part worth copying first.&lt;/p&gt;
&lt;p&gt;For a .NET application, golden scenarios can describe the behavior that must remain stable: creating a database context, authenticating an API request, scaffolding a MAUI screen, or deploying a function with the expected settings. Agents can run those scenarios after changes, while human reviewers decide whether the change fits the product and long-term architecture.&lt;/p&gt;
&lt;p&gt;Automated review should be treated as an additional signal. It can catch a missed null check or a suspicious dependency change, but it does not replace a reviewer who understands why the system exists.&lt;/p&gt;
&lt;h2 id="what-makes-a-codebase-ready"&gt;What Makes a Codebase Ready&lt;/h2&gt;
&lt;p&gt;Good structure, clear documentation, ownership information, and tests give agents something reliable to work with. Without those foundations, an agent may still produce code, but the team spends its time correcting context and assumptions.&lt;/p&gt;
&lt;p&gt;Start by making the repository easier for humans. Add a short contribution guide, make build and test commands reproducible, document service boundaries, and record architectural decisions. Those improvements help agents because they help every developer.&lt;/p&gt;
&lt;p&gt;Then introduce parallel sessions and automate one administrative task. Measure recovery time, review effort, and regression rate. The goal is not to maximize agent activity. It is to ship useful changes sooner while keeping the quality bar intact.&lt;/p&gt;
&lt;p&gt;The VS Code team&amp;rsquo;s weekly cadence is a reminder that AI-assisted development is a workflow design problem. Agents can accelerate the loop, but tests, ownership, and human taste determine whether the loop is worth running.&lt;/p&gt;</content:encoded></item><item><title>VS Code 1.111: Agent Workflows Gain Autonomy and Debug Visibility</title><link>https://thedotnetblog.com/news/emiliano-montesdeoca/vscode-1-111-release-agent-workflows/</link><pubDate>Wed, 26 Aug 2026 00:00:00 +0000</pubDate><author>Emiliano Montesdeoca</author><guid>https://thedotnetblog.com/news/emiliano-montesdeoca/vscode-1-111-release-agent-workflows/</guid><description>VS Code 1.111 adds session-level permission modes, Autopilot preview, agent-scoped hooks, and debug snapshots for safer autonomous workflows.</description><content:encoded>&lt;h1 id="vs-code-1111-agent-workflows-gain-autonomy-and-debug-visibility"&gt;VS Code 1.111: Agent Workflows Gain Autonomy and Debug Visibility&lt;/h1&gt;
&lt;p&gt;Original source: &lt;a href="https://code.visualstudio.com/updates/v1_111"&gt;Visual Studio Code 1.111&lt;/a&gt;&lt;/p&gt;
&lt;p&gt;VS Code 1.111 is the first release in the team&amp;rsquo;s move toward weekly releases, and its agent features explain why that cadence matters. The editor is making autonomous work easier to start while adding the visibility needed to understand it when something goes wrong.&lt;/p&gt;
&lt;p&gt;The headline is Autopilot, but the more important story is the boundary around autonomy: permissions, hooks, and debug snapshots give developers ways to scope and inspect the behavior.&lt;/p&gt;
&lt;h2 id="three-permission-levels-one-important-decision"&gt;Three Permission Levels, One Important Decision&lt;/h2&gt;
&lt;p&gt;The Chat view now exposes session-level permission choices. Default approvals keep confirmation behavior in place. Bypass approvals automatically accepts tool calls and retries errors. Autopilot, in preview, goes further by allowing the agent to continue, answer routine questions, retry, and stop when it signals completion.&lt;/p&gt;
&lt;p&gt;That is a real change for multi-step work. A well-scoped test update or a bounded refactor can run without a developer approving every intermediate tool call. But bypass and Autopilot also weaken the pause that normally catches a destructive command.&lt;/p&gt;
&lt;p&gt;For .NET developers, the prerequisite is not enthusiasm. It is a recovery path. Use a feature branch or isolated worktree, make sure tests and CI gates run, and keep sensitive credentials and deployment targets outside the agent&amp;rsquo;s default reach. Autonomy is only useful when the cost of a wrong decision is contained.&lt;/p&gt;
&lt;p&gt;Start with tasks whose acceptance criteria are executable: add tests for a known bug, update a set of mechanical call sites, or implement a small change with an existing pattern. Do not begin with an architectural migration that has no crisp completion signal.&lt;/p&gt;
&lt;h2 id="agent-scoped-hooks-keep-policies-local"&gt;Agent-Scoped Hooks Keep Policies Local&lt;/h2&gt;
&lt;p&gt;Hooks can now be attached to a specific agent definition rather than applied globally. This lets a specialized agent carry its own pre- and post-processing behavior without changing every other chat session.&lt;/p&gt;
&lt;p&gt;A .NET refactoring agent could run formatting and targeted tests after an edit. A database-review agent could inspect migration output without being allowed to apply it. The key is that the policy travels with the agent&amp;rsquo;s purpose.&lt;/p&gt;
&lt;p&gt;Preview syntax and behavior deserve testing in a non-critical repository. Keep hooks fast, make failures visible, and document the commands they run. A hook that quietly changes files or hides a failed validation is worse than no hook at all.&lt;/p&gt;
&lt;h2 id="debug-snapshots-turn-failures-into-evidence"&gt;Debug Snapshots Turn Failures Into Evidence&lt;/h2&gt;
&lt;p&gt;The Agent Debug panel can provide event snapshots that developers attach to chat for analysis. Instead of saying that an agent &amp;ldquo;did the wrong thing,&amp;rdquo; you can inspect the tools called, the returned data, and the point where the workflow diverged.&lt;/p&gt;
&lt;p&gt;This is particularly useful when an agent misses a project instruction or chooses the wrong configuration file. The snapshot gives the team something concrete to review and improves the next task definition or custom agent.&lt;/p&gt;
&lt;p&gt;Debug evidence also helps with reproducibility. Capture the relevant session state, compare it with the repository&amp;rsquo;s instructions, and ask whether the failure came from missing context, an overly broad tool permission, or a flawed assumption. That is a much better feedback loop than simply retrying.&lt;/p&gt;
&lt;h2 id="small-release-details-matter"&gt;Small Release Details Matter&lt;/h2&gt;
&lt;p&gt;The redesigned chat tips help surface commands such as &lt;code&gt;/init&lt;/code&gt; and &lt;code&gt;/fork&lt;/code&gt;, while AI-oriented terminal profiles can be grouped for easier access. These are not the main features, but they reduce the friction of discovering and moving between the new workflow pieces.&lt;/p&gt;
&lt;p&gt;The weekly release cadence also changes how teams should evaluate the editor. Pin versions for controlled environments, test updates on a representative repository, and keep an eye on preview features before making them part of a standard developer image.&lt;/p&gt;
&lt;h2 id="what-net-teams-should-do"&gt;What .NET Teams Should Do&lt;/h2&gt;
&lt;ol&gt;
&lt;li&gt;Create a feature-branch workflow for trying Bypass approvals or Autopilot.&lt;/li&gt;
&lt;li&gt;Use Autopilot only with explicit acceptance criteria and automated checks.&lt;/li&gt;
&lt;li&gt;Add one narrowly scoped agent hook for formatting or targeted tests.&lt;/li&gt;
&lt;li&gt;Capture debug snapshots when a session fails instead of relying on memory.&lt;/li&gt;
&lt;li&gt;Keep preview settings out of production-like automation until their behavior is understood.&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;VS Code 1.111 does not turn autonomy into a default. It makes autonomy a configurable tool. The teams that benefit will pair that tool with isolation, tests, and a clear human review boundary.&lt;/p&gt;</content:encoded></item><item><title>Making Agents Practical for Real-World Development</title><link>https://thedotnetblog.com/news/emiliano-montesdeoca/vscode-making-agents-practical/</link><pubDate>Tue, 25 Aug 2026 00:00:00 +0000</pubDate><author>Emiliano Montesdeoca</author><guid>https://thedotnetblog.com/news/emiliano-montesdeoca/vscode-making-agents-practical/</guid><description>VS Code's agent improvements address scale, memory, steering, hooks, browser verification, and reusable skills for multi-turn development work.</description><content:encoded>&lt;h1 id="making-agents-practical-for-real-world-development"&gt;Making Agents Practical for Real-World Development&lt;/h1&gt;
&lt;p&gt;Original source: &lt;a href="https://code.visualstudio.com/blogs/2026/03/05/making-agents-practical-for-real-world-development"&gt;Making agents practical for real-world development&lt;/a&gt;&lt;/p&gt;
&lt;p&gt;The gap between an impressive agent demo and a useful Thursday afternoon is mostly operational. Real repositories have large outputs, accumulated conventions, interruptions, failed commands, and decisions that need to survive more than one chat turn.&lt;/p&gt;
&lt;p&gt;VS Code&amp;rsquo;s recent agent work is aimed at that gap. The useful story is not one spectacular capability. It is the collection of small controls that make an agent easier to supervise across a real development loop.&lt;/p&gt;
&lt;h2 id="scale-the-output-without-flooding-the-session"&gt;Scale the Output Without Flooding the Session&lt;/h2&gt;
&lt;p&gt;Large terminal output and generated files can overwhelm a context window. VS Code now streams large outputs to temporary files and makes terminal output collapsible, so the conversation can remain focused on the decision rather than displaying every line.&lt;/p&gt;
&lt;p&gt;That is relevant to .NET projects with verbose restores, test runs, generated migrations, and build logs. The agent can work with the result without turning the conversation into a wall of output.&lt;/p&gt;
&lt;p&gt;Streaming does not solve architectural discovery. An agent still needs the relevant project structure, domain rules, and design constraints. If a migration touches a large model, point the agent to the schema and tests that define the intended behavior. Output management helps the context window; it does not create missing context.&lt;/p&gt;
&lt;p&gt;A good practice is to keep large artifacts inspectable and named. Ask the agent to summarize the result, identify the file containing the full output, and state which lines or failures drove its next action.&lt;/p&gt;
&lt;h2 id="memory-needs-human-direction"&gt;Memory Needs Human Direction&lt;/h2&gt;
&lt;p&gt;Agent memory can carry useful lessons across sessions, but the developer still needs to decide what matters. Manual context compaction lets you preserve specific decisions instead of trusting an automatic summary to retain every important detail.&lt;/p&gt;
&lt;p&gt;For a long-running .NET feature, write instructions such as preserving the authentication flow decision or the database transaction boundary when compacting. Then verify the next response against the actual code. Memory should reduce repetition, not become an invisible source of truth.&lt;/p&gt;
&lt;p&gt;The same principle applies to the repository. If a convention is important, encode it in tests, documentation, or project instructions. A chat memory can help today; source control is what the whole team can inspect tomorrow.&lt;/p&gt;
&lt;h2 id="redirect-without-throwing-away-the-work"&gt;Redirect Without Throwing Away the Work&lt;/h2&gt;
&lt;p&gt;An agent that takes the wrong direction creates an awkward choice: wait for it to finish, cancel it, or start over. Steering and message queueing make that choice less costly. You can redirect the next step while allowing the current operation to finish when it is safe, or queue a follow-up when the plan is still sound.&lt;/p&gt;
&lt;p&gt;Forking a chat session is useful when an architectural question has multiple plausible answers. Explore one dependency injection or data-access approach in a fork, keep the original available, and compare the resulting plans before changing the repository.&lt;/p&gt;
&lt;p&gt;These controls do not make generated decisions correct. They make supervision cheaper, which is a more durable improvement.&lt;/p&gt;
&lt;h2 id="hooks-encode-team-standards"&gt;Hooks Encode Team Standards&lt;/h2&gt;
&lt;p&gt;Hooks run at lifecycle points such as session start, before edits, or after tool calls. They turn a repeated reminder into a repository-level guardrail. A hook can request formatting, block a sensitive file change, or trigger a validation action.&lt;/p&gt;
&lt;p&gt;For a .NET team, use hooks carefully. A formatting check may be a good default. Blocking edits to a deployment file may also be sensible. A hook that runs a long integration suite before every small edit will frustrate developers and encourage them to disable the whole system.&lt;/p&gt;
&lt;p&gt;Keep the policy close to the risk. Make the hook&amp;rsquo;s failure message actionable, and ensure the command works in the same environment as the agent. Hooks are automation, so they deserve the same review as build scripts.&lt;/p&gt;
&lt;h2 id="browser-tools-verify-the-running-result"&gt;Browser Tools Verify the Running Result&lt;/h2&gt;
&lt;p&gt;Browser agent tools can navigate, interact with, and capture screenshots of a running web application. For ASP.NET Core and Blazor developers, that provides a path from generated UI code to an observable result inside the editor.&lt;/p&gt;
&lt;p&gt;Use it for bounded checks: does the route load, does the component display the expected test data, does a form show the validation state? Keep authentication, real customer data, and destructive actions outside the experiment. A browser screenshot is evidence of one state, not a replacement for automated tests or accessibility review.&lt;/p&gt;
&lt;h2 id="skills-make-good-workflows-repeatable"&gt;Skills Make Good Workflows Repeatable&lt;/h2&gt;
&lt;p&gt;Reusable skills package procedures that teams otherwise restate in every session. A .NET team might define one for preparing an Entity Framework migration, another for adding OpenAPI documentation, and another for reviewing asynchronous code.&lt;/p&gt;
&lt;p&gt;The best skill has a narrow trigger, known inputs, and a validation command. Start with a procedure that is repetitive but not ambiguous. A skill should make team knowledge easier to apply, not hide a complicated decision behind a slash command.&lt;/p&gt;
&lt;h2 id="the-practical-recommendation"&gt;The Practical Recommendation&lt;/h2&gt;
&lt;p&gt;Adopt these capabilities in layers. First make large outputs and session steering comfortable. Then add one hook and one project-specific skill. Finally, try browser verification for a local web workflow.&lt;/p&gt;
&lt;p&gt;Measure the result by review effort and recovery time, not by how autonomous the agent appears. Practical agents are the ones a team can understand, redirect, and validate when the happy path ends.&lt;/p&gt;</content:encoded></item><item><title>VS Code 1.110: Making Agents Practical for Complex Tasks</title><link>https://thedotnetblog.com/news/emiliano-montesdeoca/vscode-1-110-agent-improvements/</link><pubDate>Mon, 24 Aug 2026 00:00:00 +0000</pubDate><author>Emiliano Montesdeoca</author><guid>https://thedotnetblog.com/news/emiliano-montesdeoca/vscode-1-110-agent-improvements/</guid><description>The February release brings better agent visibility, session persistence, browser verification, and extensibility to long-running .NET development workflows.</description><content:encoded>&lt;h1 id="vs-code-1110-making-agents-practical-for-complex-tasks"&gt;VS Code 1.110: Making Agents Practical for Complex Tasks&lt;/h1&gt;
&lt;p&gt;Original source: &lt;a href="https://code.visualstudio.com/updates/v1_110"&gt;February 2026 (version 1.110)&lt;/a&gt;&lt;/p&gt;
&lt;p&gt;VS Code 1.110 focuses on the part of agent development that demos tend to skip: operating an agent once the task is large, multi-step, and connected to a real codebase. The release adds more visibility into execution, better ways to preserve context, browser tools for verification, and extensibility that can be packaged for reuse.&lt;/p&gt;
&lt;p&gt;For .NET developers, that changes the question from &amp;ldquo;can an agent generate this?&amp;rdquo; to &amp;ldquo;can I supervise this workflow without guessing what happened?&amp;rdquo;&lt;/p&gt;
&lt;h2 id="more-visibility-into-agent-execution"&gt;More Visibility Into Agent Execution&lt;/h2&gt;
&lt;p&gt;The Agent Debug panel replaces the older diagnostics-oriented experience with a view of the session&amp;rsquo;s activity. It shows which prompt files, skills, hooks, and custom configurations were loaded, along with the order of important events and tool calls.&lt;/p&gt;
&lt;p&gt;That is useful even when the agent appears to be behaving correctly. If a custom instruction did not load, a tool received an unexpected parameter, or a hook ran at the wrong point, the debug view gives you evidence instead of a vague failure. For a .NET repository with project instructions, analyzers, and multiple services, that distinction matters.&lt;/p&gt;
&lt;p&gt;Use the panel when developing custom agents or skills. Confirm the intended files were loaded, inspect the tool boundary, and keep a record of what the session actually did. It is especially valuable when two agents are running in parallel and their results seem inconsistent.&lt;/p&gt;
&lt;p&gt;The permission model is also easier to control from a session. Commands such as &lt;code&gt;/autoApprove&lt;/code&gt; and &lt;code&gt;/disableAutoApprove&lt;/code&gt; make the autonomy decision explicit in the conversation. That convenience comes with a serious warning: skipping confirmations allows terminal commands and edits to proceed without an approval pause. Enable it only after the task and repository boundary are clear.&lt;/p&gt;
&lt;h2 id="browser-verification-closes-a-loop"&gt;Browser Verification Closes a Loop&lt;/h2&gt;
&lt;p&gt;The release adds agentic browser tools for navigating, interacting with, and taking screenshots of web pages from the editor. An agent can make a change, inspect the running application, and use what it sees to guide the next iteration.&lt;/p&gt;
&lt;p&gt;For ASP.NET Core and Blazor work, this can shorten the distance between a generated UI change and a verified result. A developer can ask an agent to implement a component, start the local application, and inspect the rendered page rather than relying only on markup and compiler output.&lt;/p&gt;
&lt;p&gt;The feature is experimental, and organization administrators can control it. Browser support also depends on the model and tool configuration. Treat it as a verification aid, not as permission to skip human review. A screenshot can show that a page renders; it does not prove that authorization, accessibility, data validation, or error handling are correct.&lt;/p&gt;
&lt;p&gt;Keep browser checks bounded. Use test data, avoid real production sessions, and make the expected state explicit. The more powerful the browser tool becomes, the more important the environment boundary is.&lt;/p&gt;
&lt;h2 id="preserve-context-without-losing-control"&gt;Preserve Context Without Losing Control&lt;/h2&gt;
&lt;p&gt;Long-running sessions become easier to manage with plan memory, manual &lt;code&gt;/compact&lt;/code&gt; control, and chat forking. Instead of relying entirely on automatic compaction, a developer can decide what should survive a context reduction. Forking lets you explore an alternative design without destroying the original conversation.&lt;/p&gt;
&lt;p&gt;That is a good fit for multi-day .NET work. Preserve decisions about a database schema or API contract, fork before exploring a different dependency injection approach, and keep the original session available as a reference.&lt;/p&gt;
&lt;p&gt;Memory is not a substitute for durable project documentation. If a decision matters after the chat is gone, put it in the repository: an ADR, a design note, a test, or a project instruction. Session memory is useful for continuity; source control remains the durable record.&lt;/p&gt;
&lt;h2 id="plugins-and-skills-make-repetition-explicit"&gt;Plugins and Skills Make Repetition Explicit&lt;/h2&gt;
&lt;p&gt;Agent plugins package skills, commands, agents, MCP servers, and hooks. The release also supports creating customizations from chat through commands such as &lt;code&gt;/create-prompt&lt;/code&gt;, &lt;code&gt;/create-skill&lt;/code&gt;, &lt;code&gt;/create-agent&lt;/code&gt;, and &lt;code&gt;/create-hook&lt;/code&gt;.&lt;/p&gt;
&lt;p&gt;For .NET teams, this opens a practical path for repeatable work: a skill for adding an Entity Framework migration, a hook that runs formatting checks, or an agent that reviews an ASP.NET endpoint against local conventions. Start with a workflow you already repeat and make its inputs and outputs explicit.&lt;/p&gt;
&lt;p&gt;Do not package a vague instruction and call it automation. A useful skill states what it may inspect, what it may change, and which validation command must pass.&lt;/p&gt;
&lt;h2 id="recommendations-for-net-developers"&gt;Recommendations for .NET Developers&lt;/h2&gt;
&lt;ol&gt;
&lt;li&gt;Use the Agent Debug panel when building custom skills or troubleshooting an unexpected tool call.&lt;/li&gt;
&lt;li&gt;Try browser tools against a local ASP.NET or Blazor app with non-sensitive test data.&lt;/li&gt;
&lt;li&gt;Use &lt;code&gt;/compact&lt;/code&gt; deliberately and record durable architectural decisions in the repository.&lt;/li&gt;
&lt;li&gt;Keep auto-approval scoped to trusted, disposable environments and bounded tasks.&lt;/li&gt;
&lt;li&gt;Turn repeated .NET workflows into small skills with explicit validation.&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;VS Code 1.110 is a maturity release. The headline is not that agents can do more; it is that developers can see, interrupt, preserve, and verify more of what agents do.&lt;/p&gt;</content:encoded></item><item><title>Long-Distance Next Edit Suggestions Understand Refactoring Patterns</title><link>https://thedotnetblog.com/news/emiliano-montesdeoca/vscode-long-distance-next-edit-suggestions/</link><pubDate>Sat, 22 Aug 2026 00:00:00 +0000</pubDate><author>Emiliano Montesdeoca</author><guid>https://thedotnetblog.com/news/emiliano-montesdeoca/vscode-long-distance-next-edit-suggestions/</guid><description>VS Code's next-edit suggestions can look farther across a file to predict related changes. Here is where the feature helps .NET refactors and where it stops.</description><content:encoded>&lt;p&gt;Original source: &lt;a href="https://code.visualstudio.com/blogs/2026/02/26/long-distance-nes"&gt;Building Long-Distance Next Edit Suggestions&lt;/a&gt;&lt;/p&gt;
&lt;h2 id="refactoring-does-not-stay-near-the-cursor"&gt;Refactoring Does Not Stay Near the Cursor&lt;/h2&gt;
&lt;p&gt;Next edit suggestions are useful when the next change is close to the current one. A field initialization, a related assignment, or a small follow-up edit can appear where the developer is already working.&lt;/p&gt;
&lt;p&gt;Real refactoring is often less local. Rename a method and a call site farther down the same file needs attention. Change a parameter and a validation branch later in the file may no longer make sense. The developer has to scroll, find the related code, and wait for a new suggestion.&lt;/p&gt;
&lt;p&gt;Long-distance next edit suggestions aim at that gap. The feature predicts where the next meaningful edit may occur within the file, rather than limiting the search to the immediate neighborhood. For .NET developers doing API renames, signature changes, or validation refactors, that can remove some tedious navigation.&lt;/p&gt;
&lt;h2 id="separate-models-for-location-and-content"&gt;Separate Models for Location and Content&lt;/h2&gt;
&lt;p&gt;The engineering approach is notable because it separates two decisions. A location model predicts where the next edit belongs, while the existing next-edit model generates the edit itself.&lt;/p&gt;
&lt;p&gt;That division reflects the actual problem. Finding the right location is a different skill from proposing a syntactically plausible change. A model trained to recognize the spatial pattern of a refactor can look for a distant caller, while the edit model can focus on the local code around that caller.&lt;/p&gt;
&lt;p&gt;The training data also follows editing trajectories instead of treating every edit as an isolated event. That matters because developers usually make related changes in a sequence: rename a declaration, update a use, adjust a test, and then fix documentation or diagnostics. The sequence contains information about intent.&lt;/p&gt;
&lt;p&gt;The system also needs to know when not to jump. A false distant suggestion is more disruptive than a missed one. The source describes evaluation that measures both jump and no-jump behavior, along with tuning to reduce unnecessary jumps.&lt;/p&gt;
&lt;h2 id="why-this-is-useful-in-net"&gt;Why This Is Useful in .NET&lt;/h2&gt;
&lt;p&gt;The feature is most promising for edits with a clear ripple pattern.&lt;/p&gt;
&lt;p&gt;&lt;strong&gt;Mass renames&lt;/strong&gt; are the obvious example. If a private helper or a method local is renamed, a distant use in the same file can be surfaced without a manual search.&lt;/p&gt;
&lt;p&gt;&lt;strong&gt;Signature changes&lt;/strong&gt; are another. When a method gains a parameter, related calls or validation logic may need a corresponding edit. A suggestion that points to the next affected location can shorten the loop between compiler feedback and code change.&lt;/p&gt;
&lt;p&gt;&lt;strong&gt;In-file structural refactors&lt;/strong&gt; also benefit. Moving a block, adjusting a type, or changing a repeated pattern often creates follow-up work below the current cursor.&lt;/p&gt;
&lt;p&gt;The feature is not a replacement for the compiler, analyzers, or a deliberate search. Those tools remain authoritative for finding every reference, especially across a multi-project solution.&lt;/p&gt;
&lt;h2 id="the-trust-tradeoff"&gt;The Trust Tradeoff&lt;/h2&gt;
&lt;p&gt;The source reports a lift in code written through next-edit suggestions, but it also notes that distant suggestions are rejected more often than standard suggestions. That is understandable. A distant suggestion asks the developer to trust a model&amp;rsquo;s interpretation of a broader editing pattern.&lt;/p&gt;
&lt;p&gt;A compact preview near the cursor is a sensible interaction. It gives enough context to decide whether the proposed jump is relevant without taking over the screen. For .NET refactors, inspect the surrounding method and run the compiler or tests before treating the suggestion as complete.&lt;/p&gt;
&lt;p&gt;There are important boundaries. The feature works within a single file, so it does not solve a public type rename across a solution. It is also more reliable for straightforward patterns than for changes involving subtle validation, error handling, or cross-cutting behavior. A codebase with unusual conventions may not resemble the trajectories used for training.&lt;/p&gt;
&lt;h2 id="how-to-use-it-responsibly"&gt;How to Use It Responsibly&lt;/h2&gt;
&lt;p&gt;Enable the extended-range setting for a refactor-heavy session and start with a change that is easy to verify. Rename a method in a test file, update a signature in one class, or revise a repeated validation pattern. Compare the suggestions with compiler errors, Find All References, and tests.&lt;/p&gt;
&lt;p&gt;Do not accept a distant edit merely because it appears in a polished widget. Treat it as a proposed navigation shortcut. The quality bar is the same as for any generated code: the change must match the intended behavior and pass the repository&amp;rsquo;s checks.&lt;/p&gt;
&lt;p&gt;Long-distance next-edit suggestions are a focused improvement rather than a new programming model. For .NET teams that do frequent in-file refactoring, the saved scrolling and context recovery can compound. The feature earns trust when it helps developers reach the next correct edit while keeping the compiler and human review in charge.&lt;/p&gt;</content:encoded></item><item><title>VS Code Becomes a Home for Multi-Agent Development</title><link>https://thedotnetblog.com/news/emiliano-montesdeoca/vscode-home-for-multi-agent-development/</link><pubDate>Fri, 21 Aug 2026 00:00:00 +0000</pubDate><author>Emiliano Montesdeoca</author><guid>https://thedotnetblog.com/news/emiliano-montesdeoca/vscode-home-for-multi-agent-development/</guid><description>A unified place for local, background, cloud, and third-party coding agents changes how .NET teams divide work and review results.</description><content:encoded>&lt;p&gt;Original source: &lt;a href="https://code.visualstudio.com/blogs/2026/02/05/multi-agent-development"&gt;Your Home for Multi-Agent Development&lt;/a&gt;&lt;/p&gt;
&lt;h2 id="agent-sprawl-is-now-a-workflow-problem"&gt;Agent Sprawl Is Now a Workflow Problem&lt;/h2&gt;
&lt;p&gt;The interesting question is no longer which coding agent wins. Developers can use several agents with different strengths, and the hard part becomes coordinating them without losing context or accountability.&lt;/p&gt;
&lt;p&gt;VS Code&amp;rsquo;s multi-agent direction is an answer to that operational problem. The editor brings local, background, cloud, and third-party sessions into a common experience. For .NET teams moving from occasional chat prompts to repeatable engineering workflows, that shared view is more important than another model selector.&lt;/p&gt;
&lt;p&gt;A local agent is useful when the developer wants to steer every step. A background agent is useful for an isolated task that can run while the developer continues. A cloud agent is useful when the work needs remote infrastructure, pull-request integration, or team visibility. The same repository can need all three in one day.&lt;/p&gt;
&lt;h2 id="pick-the-agent-for-the-task"&gt;Pick the Agent for the Task&lt;/h2&gt;
&lt;p&gt;A useful way to adopt this model is to stop asking which agent should own the project and instead ask what kind of work is being done.&lt;/p&gt;
&lt;p&gt;For architecture and API design, a second model can challenge assumptions and expose tradeoffs before code is written. For repository-specific refactoring, the agent with the strongest workspace context may be the better fit because it can follow existing .NET abstractions and conventions. For a long-running change, a background or cloud session can produce a pull request while the developer handles a separate issue.&lt;/p&gt;
&lt;p&gt;The point is not to treat model differences as a leaderboard. It is to make them part of task design. A plan should say what the agent needs to know, what it may change, and how the result will be checked.&lt;/p&gt;
&lt;h2 id="subagents-add-structure-not-magic"&gt;Subagents Add Structure, Not Magic&lt;/h2&gt;
&lt;p&gt;Specialized subagents make the pattern more powerful. A parent agent can delegate a security review, a documentation scan, and an implementation investigation to focused workers, then integrate their findings.&lt;/p&gt;
&lt;p&gt;For a .NET service, those boundaries might look like this:&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;a read-only agent maps authentication and authorization paths;&lt;/li&gt;
&lt;li&gt;a test-focused agent identifies missing coverage around the proposed change;&lt;/li&gt;
&lt;li&gt;an implementation agent drafts code inside an isolated worktree;&lt;/li&gt;
&lt;li&gt;a reviewer checks the resulting diff against the original acceptance criteria.&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;This structure reduces the temptation to ask one general agent to do everything. It also gives humans clearer checkpoints. The security agent should not silently modify production configuration, and the implementation agent should not decide that an unresolved authorization question is harmless.&lt;/p&gt;
&lt;p&gt;Agent Skills extend the same idea by packaging reusable instructions and capabilities. For .NET, the opportunity is obvious: skills for API versioning, Entity Framework migrations, ASP.NET performance, or test design could make a general agent more consistent without pretending that a prompt replaces engineering experience.&lt;/p&gt;
&lt;h2 id="the-cost-of-coordination"&gt;The Cost of Coordination&lt;/h2&gt;
&lt;p&gt;Multi-agent workflows have a failure mode that single-agent workflows hide: conflicting context. One agent may assume a service uses managed identity while another assumes a connection string. One may optimize for a fast patch while another recommends an architectural migration.&lt;/p&gt;
&lt;p&gt;Use explicit handoffs. Require each agent to state assumptions, evidence, files inspected, and unresolved questions. Keep the parent session responsible for choosing between competing recommendations. For sensitive changes, make the final decision a human-reviewed pull request rather than an automatic merge.&lt;/p&gt;
&lt;p&gt;Quota and cost also become more visible. Parallel work consumes requests faster, and cloud agents may have separate availability or subscription requirements. Measure the time saved rather than assuming that more agents means more throughput.&lt;/p&gt;
&lt;h2 id="a-practical-adoption-path"&gt;A Practical Adoption Path&lt;/h2&gt;
&lt;p&gt;Start with two roles: a local agent for interactive work and a background or cloud agent for an isolated, well-scoped task. Compare the results on a small refactoring or test-improvement issue. Add a specialized subagent only after the handoff between those two roles is clear.&lt;/p&gt;
&lt;p&gt;For the first month, track which tasks are delegated successfully, how often a reviewer has to redirect the work, and whether the generated tests catch regressions. That data is more useful than a general impression that one model feels smarter.&lt;/p&gt;
&lt;p&gt;VS Code is becoming a home for multi-agent development because it gives these sessions a common operating surface. The engineering discipline still comes from the team: clear tasks, narrow permissions, visible assumptions, and tests that remain independent of whichever agent produced the code.&lt;/p&gt;</content:encoded></item><item><title>VS Code 1.109 Makes Multi-Agent Workflows Practical</title><link>https://thedotnetblog.com/news/emiliano-montesdeoca/vscode-1-109-agent-workflows/</link><pubDate>Thu, 20 Aug 2026 00:00:00 +0000</pubDate><author>Emiliano Montesdeoca</author><guid>https://thedotnetblog.com/news/emiliano-montesdeoca/vscode-1-109-agent-workflows/</guid><description>VS Code 1.109 brings multiple coding agents, session management, parallel subagents, and better steering into one developer workflow.</description><content:encoded>&lt;p&gt;Original source: &lt;a href="https://code.visualstudio.com/updates/v1_109"&gt;January 2026 (version 1.109)&lt;/a&gt;&lt;/p&gt;
&lt;h2 id="the-agent-choice-becomes-a-workflow-choice"&gt;The Agent Choice Becomes a Workflow Choice&lt;/h2&gt;
&lt;p&gt;For a while, using an AI coding assistant meant choosing the model or subscription attached to your editor and staying with it. VS Code 1.109 moves the decision to the task level. Claude, Codex, and Copilot can take different roles, while the Agent Sessions view gives you one place to see the work.&lt;/p&gt;
&lt;p&gt;That matters for .NET teams because the best agent depends on the boundary of the problem. A codebase-specific change benefits from repository context and established conventions. An architectural question may benefit from a second model&amp;rsquo;s perspective. A long-running refactor may be better delegated to a background or cloud session while the developer continues working locally.&lt;/p&gt;
&lt;p&gt;The release does not remove the need for judgment. It makes the judgment visible and easier to manage.&lt;/p&gt;
&lt;h2 id="agent-sessions-are-the-operational-layer"&gt;Agent Sessions Are the Operational Layer&lt;/h2&gt;
&lt;p&gt;The important feature is not simply that more agents are available. It is that their sessions are represented together. A developer can start a local task, inspect a background task, and return to a cloud session without keeping a separate mental checklist of terminals, browser tabs, and chat windows.&lt;/p&gt;
&lt;p&gt;For a .NET repository, imagine splitting a modernization task into three pieces: one session inventories target frameworks and packages, another examines test coverage, and a third proposes the implementation plan. The value comes from keeping those activities distinct while retaining a shared place to review their status.&lt;/p&gt;
&lt;p&gt;This also creates a better stopping rule. A session that is waiting on a tool call, has failed a test, or needs a decision should be visible as such. That is more useful than treating every agent as an opaque request-and-response interaction.&lt;/p&gt;
&lt;h2 id="parallel-subagents-change-decomposition"&gt;Parallel Subagents Change Decomposition&lt;/h2&gt;
&lt;p&gt;The release also makes parallel subagent work more practical. Instead of asking one agent to research authentication, scan project conventions, and review documentation sequentially, a parent workflow can delegate focused investigations and combine the results.&lt;/p&gt;
&lt;p&gt;The pattern works well when the tasks are genuinely independent. A .NET microservice change might need a scan of dependency injection patterns, an inventory of database migrations, and a review of CI configuration. Those investigations can run in parallel, then feed a plan that a human reviews before implementation begins.&lt;/p&gt;
&lt;p&gt;The caveat is that parallelism increases the need for clear boundaries. Each subagent needs a narrow question, a defined output, and an explicit instruction about what it may change. Otherwise, the main agent receives several plausible but incompatible recommendations. Parallel work is a coordination tool, not a substitute for task design.&lt;/p&gt;
&lt;h2 id="thinking-and-steering-need-calibration"&gt;Thinking and Steering Need Calibration&lt;/h2&gt;
&lt;p&gt;The release exposes more of the reasoning experience for supported Claude workflows and adds ways to steer or queue messages while an agent is working. Both features address a real supervision problem: developers often realize halfway through a task that the agent has misunderstood the goal.&lt;/p&gt;
&lt;p&gt;Steering is useful when the current tool call should finish but the next step needs to change. Queueing is useful when the direction is right and you want to add a follow-up without interrupting the current operation. Cancellation remains the correct choice when the agent is acting on the wrong target or the next mutation would be unsafe.&lt;/p&gt;
&lt;p&gt;More visible reasoning can help during an architecture or security review, but it is not proof that a conclusion is correct. For quick edits, the extra detail may add noise and consume context. Choose the level of visibility based on the risk of the task.&lt;/p&gt;
&lt;h2 id="what-this-means-for-net-teams"&gt;What This Means for .NET Teams&lt;/h2&gt;
&lt;p&gt;Start with a small, repeatable workflow. Use a local Copilot session for a bounded change, ask a second agent to review the plan, and use a background session only for work that has clear acceptance criteria. Keep source control and tests as the final authority.&lt;/p&gt;
&lt;p&gt;Document which tasks may run in parallel and which require a single reviewer. A good default is to allow parallel read-only investigation, but require approval before an agent edits project files, changes package versions, or runs deployment commands.&lt;/p&gt;
&lt;p&gt;VS Code 1.109 makes multi-agent development easier to operate, not magically reliable. The teams that benefit most will be the ones that pair session visibility with clear issue descriptions, reproducible builds, and a review boundary that does not disappear just because several agents can work at once.&lt;/p&gt;</content:encoded></item><item><title>VS Code Implements Full MCP Specification: Enterprise Security Meets Extensibility</title><link>https://thedotnetblog.com/news/emiliano-montesdeoca/complete-mcp-spec-in-vs-code/</link><pubDate>Wed, 12 Aug 2026 00:00:00 +0000</pubDate><author>Emiliano Montesdeoca</author><guid>https://thedotnetblog.com/news/emiliano-montesdeoca/complete-mcp-spec-in-vs-code/</guid><description>Complete Model Context Protocol support arrives with authorization, prompts, resources, and sampling. A milestone in standardized AI agent integration and enterprise-grade security.</description><content:encoded>&lt;p&gt;Original source: &lt;a href="https://code.visualstudio.com/blogs/2025/06/12/full-mcp-spec-support"&gt;The Complete MCP Experience: Full Specification Support in VS Code&lt;/a&gt;&lt;/p&gt;
&lt;p&gt;When VS Code added MCP support in May, it delivered the foundation: tools and workspace awareness. In June, the editor announced support for the complete Model Context Protocol specification.&lt;/p&gt;
&lt;p&gt;That is more than another integration checkbox. Authorization, prompts, resources, and sampling turn MCP from a collection of callable tools into a protocol for secure, context-rich agent workflows. For .NET teams building internal assistants or extending development environments, the important change is architectural: external capabilities can now be designed as interoperable services instead of one-off editor extensions.&lt;/p&gt;
&lt;h2 id="authorization-moves-credentials-to-the-right-boundary"&gt;Authorization Moves Credentials to the Right Boundary&lt;/h2&gt;
&lt;p&gt;The most consequential addition is the authorization work. MCP servers often need access to protected resources, but asking every server to implement its own OAuth flow creates a large and repetitive attack surface.&lt;/p&gt;
&lt;p&gt;The specification separates the MCP server as a resource provider from the authorization server that authenticates the user. That lets a server delegate identity to an existing provider instead of storing and managing every credential itself.&lt;/p&gt;
&lt;p&gt;The source describes the collaboration behind this work across Microsoft, Anthropic, and identity companies including Okta/Auth0, Stytch, and Descope. The result is a better default for remote servers: authenticate through established identity systems and let the client mediate the user&amp;rsquo;s consent.&lt;/p&gt;
&lt;p&gt;For an enterprise .NET application, the principle is familiar. Keep business APIs responsible for their resources, keep identity in the identity boundary, and make the delegation explicit. MCP is applying that same separation to agent integrations.&lt;/p&gt;
&lt;p&gt;Streamable HTTP makes the model useful beyond a local process. A remote MCP server can scale independently while retaining a defined authentication flow. The GitHub MCP Server is the practical example from the announcement: a remote server with OAuth integration that can use VS Code&amp;rsquo;s existing GitHub authentication and account management.&lt;/p&gt;
&lt;h2 id="prompts-package-workflows-not-just-text"&gt;Prompts Package Workflows, Not Just Text&lt;/h2&gt;
&lt;p&gt;Tools are useful when an agent needs to perform one action. Prompts are useful when the user needs to start a coordinated workflow.&lt;/p&gt;
&lt;p&gt;MCP prompts can be dynamic and context-aware. A server can tailor the starting point to the current workspace or project state, and VS Code exposes server-provided prompts alongside user-defined prompts. The invocation pattern is deliberately discoverable:&lt;/p&gt;
&lt;div class="highlight"&gt;&lt;pre tabindex="0" class="chroma"&gt;&lt;code class="language-text" data-lang="text"&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;/mcp.servername.promptname
&lt;/span&gt;&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;p&gt;That matters for teams because a prompt can become a repeatable entry point for a release check, an incident investigation, or a repository review. The server still owns the workflow context, while the client provides a consistent place to invoke it.&lt;/p&gt;
&lt;p&gt;The recommendation is to treat prompts as product surfaces. Give them names that describe an outcome, document the required permissions, and make the first action understandable to a reviewer. A prompt that quietly triggers a dozen mutations is harder to trust than one that makes its plan visible.&lt;/p&gt;
&lt;h2 id="resources-make-agent-output-usable"&gt;Resources Make Agent Output Usable&lt;/h2&gt;
&lt;p&gt;Resources represent semantic information that an agent or developer can interact with directly. A screenshot returned by Playwright can be brought into the workspace, annotated, and shared. Logs returned by a debugging tool can stream live updates instead of arriving as an opaque block of text.&lt;/p&gt;
&lt;p&gt;This is an important distinction for .NET tooling. Build diagnostics, test reports, traces, and deployment observations are not merely strings. They have structure, provenance, and often a lifecycle. Modeling them as resources gives the client more options for rendering and handling them without every server inventing a custom UI.&lt;/p&gt;
&lt;p&gt;The practical design question is ownership. Decide which system is the source of truth, how long a resource remains valid, and what permissions apply when it is shared. A live resource should also have limits: update frequency, size, and cancellation behavior need to be explicit.&lt;/p&gt;
&lt;h2 id="sampling-lets-servers-ask-for-reasoning"&gt;Sampling Lets Servers Ask for Reasoning&lt;/h2&gt;
&lt;p&gt;Sampling allows an MCP server to make language-model requests through the client. Instead of every server carrying its own AI SDK, API key, and model policy, it can request reasoning through the user&amp;rsquo;s existing model access.&lt;/p&gt;
&lt;p&gt;That opens useful patterns. A database server could inspect operational data and ask the model to explain a performance anomaly. A deployment server could summarize failed rollout signals before presenting a recommendation. A multi-agent workflow could delegate a bounded reasoning step to a specialized service.&lt;/p&gt;
&lt;p&gt;The capability also creates obligations. Sampling can consume subscription quota or incur cost, and the user needs visibility into that request. Servers should explain why they need sampling, constrain the context they send, and avoid turning a simple tool call into an unbounded inference loop.&lt;/p&gt;
&lt;h2 id="what-this-means-for-net-teams"&gt;What This Means for .NET Teams&lt;/h2&gt;
&lt;p&gt;The full specification is a reason to evaluate MCP as infrastructure, but not a reason to connect every system immediately. Start with one bounded workflow and make its permissions observable.&lt;/p&gt;
&lt;p&gt;A sensible sequence is:&lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;Connect to a remote MCP server with OAuth and inspect the consent boundary.&lt;/li&gt;
&lt;li&gt;Add a read-only prompt for a workflow your team repeats manually.&lt;/li&gt;
&lt;li&gt;Return structured resources such as build logs or test artifacts.&lt;/li&gt;
&lt;li&gt;Evaluate sampling only after measuring its cost and data flow.&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;The caveat is that full client support does not make every server production-ready. Authorization providers may differ, self-hosted systems may need additional identity work, and streamed resources can grow quickly without rate limits. MCP gives the pieces; the service owner still has to design the operating model.&lt;/p&gt;
&lt;p&gt;VS Code&amp;rsquo;s complete MCP support is a signal that the protocol is maturing from an experiment into shared agent infrastructure. For .NET developers, the next move is practical: build one small, secure integration, measure it, and let the protocol earn a larger place in your architecture.&lt;/p&gt;</content:encoded></item><item><title>Your .NET Repository Needs a Context Layer Before It Needs a Bigger Prompt</title><link>https://thedotnetblog.com/news/emiliano-montesdeoca/context-custom-instructions-vscode/</link><pubDate>Sat, 08 Aug 2026 00:00:00 +0000</pubDate><author>Emiliano Montesdeoca</author><guid>https://thedotnetblog.com/news/emiliano-montesdeoca/context-custom-instructions-vscode/</guid><description>Custom instructions turn Copilot from a general assistant into a teammate that understands a .NET repository's conventions, architecture, and delivery rules.</description><content:encoded>&lt;p&gt;Original source: &lt;a href="https://code.visualstudio.com/blogs/2025/03/26/custom-instructions"&gt;Context is all you need: Better AI results with custom instructions&lt;/a&gt;&lt;/p&gt;
&lt;p&gt;Most teams try to improve AI-assisted development by writing longer prompts. That is usually the wrong first move.&lt;/p&gt;
&lt;p&gt;The better move is to give the tool a durable context layer: the conventions, architectural boundaries, commands, and review expectations that apply to every change in the repository. The Visual Studio Code team calls this custom instructions, and the idea is especially useful for .NET codebases where a short request can touch project references, analyzers, tests, hosting, and deployment decisions all at once.&lt;/p&gt;
&lt;h2 id="the-prompt-should-not-carry-the-repositorys-memory"&gt;The prompt should not carry the repository&amp;rsquo;s memory&lt;/h2&gt;
&lt;p&gt;A prompt such as &amp;ldquo;add an endpoint for orders&amp;rdquo; leaves too many decisions open. Which API style? Which validation library? Where do application services live? Are errors returned as &lt;code&gt;ProblemDetails&lt;/code&gt;? Which test project owns the behavior? Does the solution use minimal APIs, controllers, or a vertical slice layout?&lt;/p&gt;
&lt;p&gt;You can answer those questions every time, but then the prompt becomes a second configuration file. Worse, it becomes a private configuration file that only exists in one developer&amp;rsquo;s chat history.&lt;/p&gt;
&lt;p&gt;The source article&amp;rsquo;s most practical recommendation is a simple &lt;code&gt;.github/copilot-instructions.md&lt;/code&gt; file. Copilot can discover it automatically, which means the repository can state its working agreements once and let every contributor benefit from them.&lt;/p&gt;
&lt;p&gt;For a .NET solution, I would start with something like this:&lt;/p&gt;
&lt;div class="highlight"&gt;&lt;pre tabindex="0" class="chroma"&gt;&lt;code class="language-markdown" data-lang="markdown"&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;&lt;span class="gh"&gt;# Repository guidance
&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;&lt;span class="gu"&gt;## Architecture
&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;&lt;span class="k"&gt;-&lt;/span&gt; Keep HTTP concerns in the Web project.
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;&lt;span class="k"&gt;-&lt;/span&gt; Put business rules in application services or vertical slices.
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;&lt;span class="k"&gt;-&lt;/span&gt; Keep EF Core queries in the data-access boundary.
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;&lt;span class="k"&gt;-&lt;/span&gt; Do not reference infrastructure projects from the domain project.
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;&lt;span class="gu"&gt;## .NET conventions
&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;&lt;span class="k"&gt;-&lt;/span&gt; Use nullable reference types and treat warnings as errors.
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;&lt;span class="k"&gt;-&lt;/span&gt; Prefer async APIs and pass CancellationToken through application boundaries.
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;&lt;span class="k"&gt;-&lt;/span&gt; Return ProblemDetails for API errors.
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;&lt;span class="k"&gt;-&lt;/span&gt; Add or update tests for every behavior change.
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;&lt;span class="gu"&gt;## Validation
&lt;/span&gt;&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;&lt;span class="k"&gt;-&lt;/span&gt; Run &lt;span class="sb"&gt;`dotnet format --verify-no-changes`&lt;/span&gt; before opening a pull request.
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;&lt;span class="k"&gt;-&lt;/span&gt; Run &lt;span class="sb"&gt;`dotnet test --solution .`&lt;/span&gt; for the full test suite.
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;&lt;span class="k"&gt;-&lt;/span&gt; Do not introduce a new package when an existing project dependency already solves the problem.
&lt;/span&gt;&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;p&gt;This is not a replacement for analyzers, tests, or code review. It is the shared explanation around those mechanisms. The compiler can tell you that a nullable value is unsafe; the instructions can tell Copilot how this repository expects that case to be handled.&lt;/p&gt;
&lt;h2 id="keep-instructions-close-to-the-thing-they-govern"&gt;Keep instructions close to the thing they govern&lt;/h2&gt;
&lt;p&gt;One large instruction file is a useful starting point, but it can become another wall of prose. The source article also describes referencing separate files through workspace settings. That lets a team keep specialized guidance where it belongs.&lt;/p&gt;
&lt;p&gt;For example, a repository might have:&lt;/p&gt;
&lt;div class="highlight"&gt;&lt;pre tabindex="0" class="chroma"&gt;&lt;code class="language-text" data-lang="text"&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;.github/
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt; copilot-instructions.md
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;docs/
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt; api-conventions.md
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt; database-conventions.md
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt; testing-conventions.md
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;.vscode/
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt; settings.json
&lt;/span&gt;&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;p&gt;Then workspace settings can point Copilot at the relevant files:&lt;/p&gt;
&lt;div class="highlight"&gt;&lt;pre tabindex="0" class="chroma"&gt;&lt;code class="language-json" data-lang="json"&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;&lt;span class="p"&gt;{&lt;/span&gt;
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt; &lt;span class="nt"&gt;&amp;#34;github.copilot.chat.codeGeneration.instructions&amp;#34;&lt;/span&gt;&lt;span class="p"&gt;:&lt;/span&gt; &lt;span class="p"&gt;[&lt;/span&gt;
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt; &lt;span class="p"&gt;{&lt;/span&gt; &lt;span class="nt"&gt;&amp;#34;file&amp;#34;&lt;/span&gt;&lt;span class="p"&gt;:&lt;/span&gt; &lt;span class="s2"&gt;&amp;#34;./docs/api-conventions.md&amp;#34;&lt;/span&gt; &lt;span class="p"&gt;},&lt;/span&gt;
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt; &lt;span class="p"&gt;{&lt;/span&gt; &lt;span class="nt"&gt;&amp;#34;file&amp;#34;&lt;/span&gt;&lt;span class="p"&gt;:&lt;/span&gt; &lt;span class="s2"&gt;&amp;#34;./docs/database-conventions.md&amp;#34;&lt;/span&gt; &lt;span class="p"&gt;},&lt;/span&gt;
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt; &lt;span class="p"&gt;{&lt;/span&gt; &lt;span class="nt"&gt;&amp;#34;file&amp;#34;&lt;/span&gt;&lt;span class="p"&gt;:&lt;/span&gt; &lt;span class="s2"&gt;&amp;#34;./docs/testing-conventions.md&amp;#34;&lt;/span&gt; &lt;span class="p"&gt;}&lt;/span&gt;
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt; &lt;span class="p"&gt;]&lt;/span&gt;
&lt;/span&gt;&lt;/span&gt;&lt;span class="line"&gt;&lt;span class="cl"&gt;&lt;span class="p"&gt;}&lt;/span&gt;
&lt;/span&gt;&lt;/span&gt;&lt;/code&gt;&lt;/pre&gt;&lt;/div&gt;&lt;p&gt;I like this approach because it mirrors how a .NET solution is usually organized. API guidance can talk about contracts and versioning without repeating database rules. Testing guidance can explain fixture lifetimes and integration-test dependencies without making every code-generation request carry the entire platform manual.&lt;/p&gt;
&lt;p&gt;The important constraint is ownership. If a file is referenced by the workspace, it needs to be reviewed like code. Stale instructions are worse than no instructions because they make incorrect output look intentional.&lt;/p&gt;
&lt;h2 id="prompt-files-are-reusable-workflows-not-magic-spells"&gt;Prompt files are reusable workflows, not magic spells&lt;/h2&gt;
&lt;p&gt;The announcement also introduces prompt files: Markdown files under &lt;code&gt;.github/prompts&lt;/code&gt; that package a repeatable request. This is where custom instructions move from passive context to team workflow.&lt;/p&gt;
&lt;p&gt;A prompt file for a new vertical slice might ask Copilot to inspect an existing slice, create the request and handler, add validation, add tests, and report the commands it ran. Another could describe the checks for a migration or a production incident.&lt;/p&gt;
&lt;p&gt;The value is not that Copilot has learned a secret incantation. The value is that the team has made its process visible, reviewable, and repeatable.&lt;/p&gt;
&lt;p&gt;For us .NET developers, that distinction matters. A prompt file should not hide decisions that belong in code or CI. It should make the path through those decisions easier to follow. If a prompt file says &amp;ldquo;add tests,&amp;rdquo; it should name the test project, test framework, and required command. If it says &amp;ldquo;add an endpoint,&amp;rdquo; it should point to a real neighboring implementation.&lt;/p&gt;
&lt;h2 id="what-i-would-do-in-a-real-repository"&gt;What I would do in a real repository&lt;/h2&gt;
&lt;p&gt;I would roll this out in three small steps:&lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;Add a short root instruction file with architecture boundaries, commands, and non-negotiable quality rules.&lt;/li&gt;
&lt;li&gt;Add one prompt file for the most repetitive workflow, such as creating an API slice or diagnosing a failed build.&lt;/li&gt;
&lt;li&gt;Review the generated changes and the instructions together after a week of use.&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;That last step is the one teams skip. The useful question is not whether Copilot produced more code. It is whether the repository needed fewer corrections after the first draft. If the answer is no, improve the context before asking for a more elaborate prompt.&lt;/p&gt;
&lt;h2 id="the-practical-takeaway"&gt;The practical takeaway&lt;/h2&gt;
&lt;p&gt;Custom instructions are not a way to outsource engineering judgment. They are a way to keep engineering judgment from disappearing into individual conversations.&lt;/p&gt;
&lt;p&gt;Start with the repository&amp;rsquo;s boundaries, commands, and examples. Keep specialized guidance in focused files. Turn repeated workflows into prompt files. Then let tests, analyzers, and review remain the final authority.&lt;/p&gt;
&lt;p&gt;The best AI result is often the one that required the smallest prompt because the repository already explained the important parts.&lt;/p&gt;</content:encoded></item><item><title>VS Code 1.117: Agents Are Getting Their Own Git Branches and I'm Here For It</title><link>https://thedotnetblog.com/news/emiliano-montesdeoca/vscode-1-117-agents-autopilot-worktrees/</link><pubDate>Sun, 19 Apr 2026 00:00:00 +0000</pubDate><author>Emiliano Montesdeoca</author><guid>https://thedotnetblog.com/news/emiliano-montesdeoca/vscode-1-117-agents-autopilot-worktrees/</guid><description>VS Code 1.117 ships worktree isolation for agent sessions, persistent Autopilot mode, and subagent support. The agentic coding workflow just got way more real.</description><content:encoded>&lt;p&gt;The line between &amp;ldquo;AI assistant&amp;rdquo; and &amp;ldquo;AI teammate&amp;rdquo; keeps getting thinner. VS Code 1.117 just dropped and the &lt;a href="https://code.visualstudio.com/updates/v1_117"&gt;full release notes&lt;/a&gt; are packed, but the story here is clear: agents are becoming first-class citizens in your dev workflow.&lt;/p&gt;
&lt;p&gt;Here&amp;rsquo;s what actually matters.&lt;/p&gt;
&lt;h2 id="autopilot-mode-finally-remembers-your-preference"&gt;Autopilot mode finally remembers your preference&lt;/h2&gt;
&lt;p&gt;Previously, you had to re-enable Autopilot every time you started a new session. Annoying. Now your permission mode persists across sessions, and you can configure the default.&lt;/p&gt;
&lt;p&gt;The Agent Host supports three session configs:&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;&lt;strong&gt;Default&lt;/strong&gt; — tools ask for confirmation before running&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Bypass&lt;/strong&gt; — auto-approves everything&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Autopilot&lt;/strong&gt; — fully autonomous, answers its own questions and keeps going&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;If you&amp;rsquo;re scaffolding a new .NET project with migrations, Docker, and CI — set it to Autopilot once and forget about it. That preference sticks.&lt;/p&gt;
&lt;h2 id="worktree-and-git-isolation-for-agent-sessions"&gt;Worktree and git isolation for agent sessions&lt;/h2&gt;
&lt;p&gt;This is the big one. Agent sessions now support full worktree and git isolation. That means when an agent works on a task, it gets its own branch and working directory. Your main branch stays untouched.&lt;/p&gt;
&lt;p&gt;Even better — Copilot CLI generates meaningful branch names for these worktree sessions. No more &lt;code&gt;agent-session-abc123&lt;/code&gt;. You get something that actually describes what the agent is doing.&lt;/p&gt;
&lt;p&gt;For .NET developers running multiple feature branches or fixing bugs while a long scaffolding task runs, this is a game changer. You can have an agent building out your API controllers in one worktree while you&amp;rsquo;re debugging a service layer issue in another. No conflicts. No stashing. No mess.&lt;/p&gt;
&lt;h2 id="subagents-and-agent-teams"&gt;Subagents and agent teams&lt;/h2&gt;
&lt;p&gt;The Agent Host Protocol now supports subagents. An agent can spin up other agents to handle parts of a task. Think of it as delegating — your main agent coordinates, and specialized agents handle the pieces.&lt;/p&gt;
&lt;p&gt;This is early, but the potential for .NET workflows is obvious. Imagine one agent handling your EF Core migrations while another sets up your integration tests. We&amp;rsquo;re not fully there yet, but the protocol support landing now means tooling will follow fast.&lt;/p&gt;
&lt;h2 id="terminal-output-auto-included-when-agents-send-input"&gt;Terminal output auto-included when agents send input&lt;/h2&gt;
&lt;p&gt;Small but meaningful. When an agent sends input to the terminal, the terminal output is now automatically included in the context. Before, the agent had to make an extra turn just to read what happened.&lt;/p&gt;
&lt;p&gt;If you&amp;rsquo;ve ever watched an agent run &lt;code&gt;dotnet build&lt;/code&gt;, fail, and then take another round-trip just to see the error — that friction is gone. It sees the output immediately and reacts.&lt;/p&gt;
&lt;h2 id="self-updating-agents-app-on-macos"&gt;Self-updating Agents app on macOS&lt;/h2&gt;
&lt;p&gt;The standalone Agents app on macOS now self-updates. No more manually downloading new versions. It just stays current.&lt;/p&gt;
&lt;h2 id="the-smaller-stuff-worth-knowing"&gt;The smaller stuff worth knowing&lt;/h2&gt;
&lt;ul&gt;
&lt;li&gt;&lt;strong&gt;package.json hovers&lt;/strong&gt; now show both the installed version and the latest available. Useful if you manage npm tooling alongside your .NET projects.&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Images in JSDoc&lt;/strong&gt; comments render correctly in hovers and completions.&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Copilot CLI sessions&lt;/strong&gt; now indicate whether they were created by VS Code or externally — handy when you&amp;rsquo;re jumping between terminals.&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Copilot CLI, Claude Code, and Gemini CLI&lt;/strong&gt; are recognized as shell types. The editor knows what you&amp;rsquo;re running.&lt;/li&gt;
&lt;/ul&gt;
&lt;h2 id="the-takeaway"&gt;The takeaway&lt;/h2&gt;
&lt;p&gt;VS Code 1.117 isn&amp;rsquo;t a flashy feature dump. It&amp;rsquo;s infrastructure. Worktree isolation, persistent permissions, subagent protocols — these are the building blocks for a workflow where agents handle real, parallel tasks without stepping on your code.&lt;/p&gt;
&lt;p&gt;If you&amp;rsquo;re building with .NET and haven&amp;rsquo;t leaned into the agentic workflow yet, honestly, now&amp;rsquo;s the time to start.&lt;/p&gt;</content:encoded></item><item><title>VS Code 1.116 — Agents App Gets Keyboard Navigation and File Context Completions</title><link>https://thedotnetblog.com/news/emiliano-montesdeoca/vscode-1-116-agents-app-updates/</link><pubDate>Fri, 10 Apr 2026 00:00:00 +0000</pubDate><author>Emiliano Montesdeoca</author><guid>https://thedotnetblog.com/news/emiliano-montesdeoca/vscode-1-116-agents-app-updates/</guid><description>VS Code 1.116 focuses on Agents app polish — dedicated keybindings, accessibility improvements, file-context completions, and CSS @import link resolution.</description><content:encoded>&lt;p&gt;VS Code 1.116 is the April 2026 release, and while it&amp;rsquo;s lighter than some recent updates, the changes are focused and meaningful — especially if you&amp;rsquo;re using the Agents app daily.&lt;/p&gt;
&lt;p&gt;Here&amp;rsquo;s what landed, based on the &lt;a href="https://code.visualstudio.com/updates/v1_116"&gt;official release notes&lt;/a&gt;.&lt;/p&gt;
&lt;h2 id="agents-app-improvements"&gt;Agents app improvements&lt;/h2&gt;
&lt;p&gt;The Agents app continues to mature with usability polish that makes a real difference in daily workflows:&lt;/p&gt;
&lt;p&gt;&lt;strong&gt;Dedicated keybindings&lt;/strong&gt; — you can now focus the Changes view, the files tree within Changes, and the Chat Customizations view with dedicated commands and keyboard shortcuts. If you&amp;rsquo;ve been clicking around the Agents app to navigate, this brings full keyboard-driven workflows.&lt;/p&gt;
&lt;p&gt;&lt;strong&gt;Accessibility help dialog&lt;/strong&gt; — pressing &lt;code&gt;Alt+F1&lt;/code&gt; in the chat input box now opens an accessibility help dialog showing available commands and keybindings. Screen reader users can also control announcement verbosity. Good accessibility benefits everyone.&lt;/p&gt;
&lt;p&gt;&lt;strong&gt;File-context completions&lt;/strong&gt; — type &lt;code&gt;#&lt;/code&gt; in the Agents app chat to trigger file-context completions scoped to your current workspace. This is one of those small quality-of-life improvements that speeds up every interaction — no more typing full file paths when referencing code.&lt;/p&gt;
&lt;h2 id="css-import-link-resolution"&gt;CSS &lt;code&gt;@import&lt;/code&gt; link resolution&lt;/h2&gt;
&lt;p&gt;A nice one for frontend developers: VS Code now resolves CSS &lt;code&gt;@import&lt;/code&gt; references that use node_modules paths. You can &lt;code&gt;Ctrl+click&lt;/code&gt; through imports like &lt;code&gt;@import &amp;quot;some-module/style.css&amp;quot;&lt;/code&gt; when using bundlers. Small but eliminates a friction point in CSS workflows.&lt;/p&gt;
&lt;h2 id="wrapping-up"&gt;Wrapping up&lt;/h2&gt;
&lt;p&gt;VS Code 1.116 is about refinement — making the Agents app more navigable, more accessible, and more keyboard-friendly. If you&amp;rsquo;re spending significant time in the Agents app (and I suspect many of us are), these changes add up.&lt;/p&gt;
&lt;p&gt;Check the &lt;a href="https://code.visualstudio.com/updates/v1_116"&gt;full release notes&lt;/a&gt; for the complete list.&lt;/p&gt;</content:encoded></item><item><title>VS Code 1.115 — Background Terminal Notifications, SSH Agent Mode, and More</title><link>https://thedotnetblog.com/news/emiliano-montesdeoca/vscode-1-115-agent-improvements/</link><pubDate>Mon, 06 Apr 2026 00:00:00 +0000</pubDate><author>Emiliano Montesdeoca</author><guid>https://thedotnetblog.com/news/emiliano-montesdeoca/vscode-1-115-agent-improvements/</guid><description>VS Code 1.115 brings background terminal notifications for agents, SSH remote agent hosting, file paste in terminals, and session-aware edit tracking. Here's what matters for .NET developers.</description><content:encoded>&lt;p&gt;VS Code 1.115 just &lt;a href="https://code.visualstudio.com/updates/v1_115"&gt;dropped&lt;/a&gt;, and while it&amp;rsquo;s a lighter release in terms of headline features, the agent-related improvements are genuinely useful if you&amp;rsquo;re working with AI coding assistants daily.&lt;/p&gt;
&lt;p&gt;Let me highlight what&amp;rsquo;s actually worth knowing.&lt;/p&gt;
&lt;h2 id="background-terminals-talk-back-to-agents"&gt;Background terminals talk back to agents&lt;/h2&gt;
&lt;p&gt;This is the standout feature. Background terminals now automatically notify agents when commands complete, including the exit code and terminal output. Input prompts in background terminals are also detected and surfaced to the user.&lt;/p&gt;
&lt;p&gt;Why does this matter? If you&amp;rsquo;ve used Copilot&amp;rsquo;s agent mode to run build commands or test suites in the background, you know the pain of &amp;ldquo;did that finish yet?&amp;rdquo; — background terminals were essentially fire-and-forget. Now the agent gets notified when your &lt;code&gt;dotnet build&lt;/code&gt; or &lt;code&gt;dotnet test&lt;/code&gt; completes, sees the output, and can react accordingly. It&amp;rsquo;s a small change that makes agent-driven workflows significantly more reliable.&lt;/p&gt;
&lt;p&gt;There&amp;rsquo;s also a new &lt;code&gt;send_to_terminal&lt;/code&gt; tool that lets agents send commands to background terminals with user confirmation, fixing the issue where &lt;code&gt;run_in_terminal&lt;/code&gt; with a timeout would move terminals to the background and make them read-only.&lt;/p&gt;
&lt;h2 id="ssh-remote-agent-hosting"&gt;SSH remote agent hosting&lt;/h2&gt;
&lt;p&gt;VS Code now supports connecting to remote machines over SSH, automatically installing the CLI and starting it in agent host mode. This means your AI agent sessions can target remote environments directly — useful for .NET developers who build and test on Linux servers or cloud VMs.&lt;/p&gt;
&lt;h2 id="edit-tracking-in-agent-sessions"&gt;Edit tracking in agent sessions&lt;/h2&gt;
&lt;p&gt;File edits made during agent sessions are now tracked and restored, with diffs, undo/redo, and state restoration. If an agent makes changes to your code and something goes wrong, you can see exactly what changed and roll it back. Peace of mind for letting agents modify your codebase.&lt;/p&gt;
&lt;h2 id="browser-tab-awareness-and-other-improvements"&gt;Browser tab awareness and other improvements&lt;/h2&gt;
&lt;p&gt;A few more quality-of-life additions:&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;&lt;strong&gt;Browser tab tracking&lt;/strong&gt; — chat can now track and link to browser tabs opened during a session, so agents can reference web pages you&amp;rsquo;re looking at&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;File paste in terminal&lt;/strong&gt; — paste files (including images) into the terminal with Ctrl+V, drag-and-drop, or right-click&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Test coverage in minimap&lt;/strong&gt; — test coverage indicators now show in the minimap for a quick visual overview&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Pinch-to-zoom on Mac&lt;/strong&gt; — integrated browser supports pinch-to-zoom gestures&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Copilot entitlements in Sessions&lt;/strong&gt; — status bar shows usage info in the Sessions view&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Favicon in Go to File&lt;/strong&gt; — open web pages show favicons in the quick pick list&lt;/li&gt;
&lt;/ul&gt;
&lt;h2 id="wrapping-up"&gt;Wrapping up&lt;/h2&gt;
&lt;p&gt;VS Code 1.115 is an incremental release, but the agent improvements — background terminal notifications, SSH agent hosting, and edit tracking — add up to a noticeably smoother experience for AI-assisted development. If you&amp;rsquo;re using Copilot&amp;rsquo;s agent mode for .NET projects, these are the kinds of quality-of-life fixes that reduce friction daily.&lt;/p&gt;
&lt;p&gt;Check out the &lt;a href="https://code.visualstudio.com/updates/v1_115"&gt;full release notes&lt;/a&gt; for every detail.&lt;/p&gt;</content:encoded></item></channel></rss>